# Project Structural Guidelines

This reference provides the Antigravity agent with the necessary context on the project's folder hierarchy and module responsibilities.
- The `workspace/` directory is isolated per agent (e.g., `repo-guardian`, `memory-curator`).
- All execution should remain within the agent's designated workspace when possible.
